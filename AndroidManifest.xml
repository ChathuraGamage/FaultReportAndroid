<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.faultreportapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        >
        <activity
            android:name="com.example.faultreportapp.Start"
            android:label="@string/app_name" 
            android:theme="@style/pop_up" >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".Faultpage"
            	  android:theme="@style/pages" />
        
        <activity android:name=".Incidentpage" 
                  android:theme="@style/pages" />
        
        <activity android:name=".Exitconfirm" 
                  android:theme="@style/pop_up" />
              
    </application>

</manifest>